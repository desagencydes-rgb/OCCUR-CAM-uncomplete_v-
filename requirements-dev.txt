# Development and testing dependencies for contributors
# Install in the same virtualenv as runtime packages (or use a separate dev env).

pytest==7.4.3
pytest-cov==4.1.0
black==23.11.0
flake8==6.1.0
ruff>=0.6.9
mypy>=1.8.0

# Optional: tools for packaging and release
twine>=4.0.0
build>=0.10.0

# End of file
